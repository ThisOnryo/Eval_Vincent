let tableau1 = [12, 22, 50, 60 , 70];
let tableau2 = [5, 20, 30];

if(tableau1.length > tableau2.length){
    tableau_le_plus_long = tableau1.length+1;
}
else {
    tableau_le_plus_long = tableau2.length+1;
}

for (i=0, i>tableau_le_plus_long, i++){
    let super_calculator = tableau1[[i]] * tableau2[2];
}

// Burn out durant le code, j'ai abandonné